<!DOCTYPE html>
<html lang="en-US">
  <%= render 'head' %>
  
  <body>
    <%= render 'body_top' %>
    <%= render 'header' %>

    <div class="container">
      <div class="row">
        <div class="col-xs-3"></div>
        <div class="col-xs-6 offset-xs-3">
          <%= render 'flash_messages' %>
        </div>
        <div class="col-xs-3"></div>
      </div>
    </div>

    <%= yield %>
    <%= render 'footer' %>

    <div id="remote_container"></div>
    <div id="schedule_service_div"></div>

    <%#= render 'how_it_works_modal' %>
    <div class="cd-top"><i class="fa fa-chevron-up topicon"></i></div>
    <%= render 'js' %>
  </body>
</html>
