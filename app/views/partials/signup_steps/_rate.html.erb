<input type="button" name="next" class="next action-button" value="Skip" />
<input type="button" name="previous" class="previous action-button-previous" value="Back" />
<br><br><br>
<%= form_for @user, as: :user, url: "/profiles/#{@user.slug}", html: { multipart: true, autocomplete: 'off', id: "msform" }, remote: true do |f| %>
  <%= f.hidden_field :current_setting_step, value: "rate", class: 'form-control' %>

  <div class="form-card clearfix">
    <div class="row">
      <div class="col-7">
        <h2 class="fs-title">Rate</h2>
      </div>
    </div>
    <div class="col-md-12 m-b-5">
      <%= f.label :rate, "Rate per minute", class: 'ms-label' %><br />
      <%= f.select :rate, User::RATES, { :prompt => "Select Rate" } , { class: 'form-control', required: true } %>
    </div>
  </div>
  <div class="white-cont clearfix">
    <%= f.submit "Submit & Next", class: 'btn btn-primary step_submit'  %>
  </div>
<% end %>
